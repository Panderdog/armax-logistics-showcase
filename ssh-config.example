# SSH Config Example for Deploy
# Copy this to ~/.ssh/config and customize for your server

# Usage:
# 1. Copy this file's contents to ~/.ssh/config
# 2. Replace placeholders with your actual values
# 3. Use: ssh myserver (instead of ssh -p PORT user@host)

Host myserver
    HostName your-server.com        # Your server IP or domain
    User your_username              # Your SSH username
    Port 22                         # SSH port (usually 22)
    IdentityFile ~/.ssh/id_rsa      # Path to your private key
    ServerAliveInterval 60          # Keep connection alive
    ServerAliveCountMax 3

# Example for production server
Host armax-prod
    HostName 192.168.1.100
    User deploy
    Port 22
    IdentityFile ~/.ssh/armax_deploy_key
    ServerAliveInterval 60
    ServerAliveCountMax 3

# Example for staging server
Host armax-staging
    HostName staging.example.com
    User deploy
    Port 2222
    IdentityFile ~/.ssh/armax_deploy_key
    ServerAliveInterval 60
    ServerAliveCountMax 3

# After setup, you can connect with:
# ssh myserver
# Or use in deploy script by changing REMOTE_HOST to just "myserver"
