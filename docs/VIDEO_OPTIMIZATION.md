# 🎯 Оптимизация загрузки видео

## Проблема
PageSpeed Insights жаловался на чрезмерную нагрузку на сеть (~7 МБ), где **~4 МБ занимали видео файлы**, которые загружались даже на мобильных устройствах, где они вообще не показываются.

## Решение

### 1. Использование `data-src` вместо `src`
```html
<!-- ❌ БЫЛО: браузер загружает автоматически -->
<source src="/video/compress-hero-video.webm" type="video/webm" />

<!-- ✅ СТАЛО: браузер НЕ загружает -->
<source data-src="/video/compress-hero-video.webm" type="video/webm" />
```

### 2. Динамическая загрузка только на Desktop
```typescript
useEffect(() => {
  // НЕ загружаем видео во время pre-rendering (react-snap)
  if (navigator.userAgent.includes('ReactSnap')) return;
  
  // Загружаем видео только на desktop/tablet
  if (!isDesktop) return;
  
  const video = document.getElementById('hero-video') as HTMLVideoElement;
  const sources = video.querySelectorAll('source');
  
  // Копируем data-src → src ТОЛЬКО на desktop
  sources.forEach(source => {
    const dataSrc = source.getAttribute('data-src');
    if (dataSrc) {
      source.setAttribute('src', dataSrc);
    }
  });
  
  video.load(); // Запускаем загрузку
}, [isDesktop]);
```

### 3. Изменение preload атрибута
```html
<!-- ❌ БЫЛО -->
<video preload="metadata" ...>

<!-- ✅ СТАЛО -->
<video preload="none" ...>
```

## Результат

### До оптимизации
- 📱 **Mobile**: Загружало ~3.9 МБ видео файлов (которые не показываются!)
- 💻 **Desktop**: Загружало ~3.9 МБ видео файлов
- 📊 **Общая нагрузка**: ~7.2 МБ

### После оптимизации
- 📱 **Mobile**: 0 байт видео (экономия ~3.9 МБ - 54% от общей нагрузки!)
- 💻 **Desktop**: Загружает ~3.9 МБ видео файлов (как и нужно)
- 🖼️ **nightport**: webp формат (98 КБ вместо 740 КБ jpg - экономия 642 КБ!)
- 📊 **Общая нагрузка на mobile**: ~2.6 МБ (без видео + оптимизированный nightport)

## Как проверить

1. **Локально**: `npm run build && npx serve dist`
2. **DevTools**: Network → фильтр "Media" → на мобильном не должно быть видео файлов
3. **Проверить в HTML**: 
   ```bash
   grep 'data-src.*compress-hero-video' dist/index.html  # ✅ должно быть
   grep 'src="/video/compress' dist/index.html           # ❌ не должно быть
   ```

## Файлы изменены
- `src/components/HeroSection.tsx` - добавлена логика условной загрузки видео

## Дополнительные рекомендации

Для еще большей оптимизации можно:
1. ~~**Оптимизировать изображения**~~ ✅ **Сделано** - `nightport.webp` (98 КБ вместо 740 КБ jpg)
2. **Code splitting** - разделить JS bundle на более мелкие чанки
3. **Lazy loading** - загружать изображения сервисов по мере прокрутки (уже используется)
4. **CDN** - использовать CDN для статических ресурсов

## Обновления

### 2026-02-07 - Замена nightport.jpg на nightport.webp
- Заменены все упоминания `nightport.jpg` → `nightport.webp` в файлах:
  - `src/pages/Reviews.tsx`
  - `src/pages/PrivacyPolicy.tsx`
  - `src/pages/Contacts.tsx`
  - `src/pages/Geography.tsx`
  - `src/pages/About.tsx`
  - `src/pages/Index.tsx`
- **Экономия**: 642 КБ (~87% от исходного размера)
- **Было**: 740 КБ (jpg)
- **Стало**: 98 КБ (webp)

---

**Статус**: ✅ Реализовано и протестировано
**Дата**: 2026-02-07
