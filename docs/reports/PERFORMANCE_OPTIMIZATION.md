# Оптимизация производительности Hero-видео

## Проблемы (до оптимизации)
1. ❌ **Двойная загрузка видео**: Оба формата (webm и mp4) загружались одновременно
2. ❌ **Загрузка на мобиле**: Видео загружалось на мобильных устройствах, хотя не отображалось
3. ❌ **Агрессивный preload**: `preload="auto"` заставлял браузер загружать видео немедленно
4. ❌ **Медленный LCP**: Критические ресурсы (изображения, шрифты) не были приоритизированы

## Решение (после оптимизации)

### 1. Условный рендеринг видео
**Файл**: `src/components/HeroSection.tsx`

```typescript
// Видео рендерится ТОЛЬКО на desktop/tablet (≥768px)
const [isDesktop, setIsDesktop] = useState(false);

useEffect(() => {
  const mediaQuery = window.matchMedia('(min-width: 768px)');
  const handleMediaChange = (e) => setIsDesktop(e.matches);
  handleMediaChange(mediaQuery);
  mediaQuery.addEventListener('change', handleMediaChange);
  return () => mediaQuery.removeEventListener('change', handleMediaChange);
}, []);
```

**Результат**:
- ✅ На мобиле видео **не рендерится в DOM** → 0 запросов
- ✅ На desktop/tablet видео загружается корректно

### 2. Оптимизация preload
**Изменение**: `preload="auto"` → `preload="metadata"`

```html
<video preload="metadata" ... >
```

**Результат**:
- ✅ Браузер загружает только метаданные (размер, длительность)
- ✅ Полное видео загружается только при необходимости
- ✅ Экономия трафика и ускорение первоначальной загрузки

### 3. Preload критических ресурсов
**Файл**: `index.html`

```html
<!-- Критический шрифт для заголовка -->
<link rel="preload" as="font" type="font/woff2" 
  href="/fonts/plus-jakarta-sans/plus-jakarta-sans-cyrillic-700-normal.woff2" 
  crossorigin="anonymous" />
<link rel="preload" as="font" type="font/woff2" 
  href="/fonts/plus-jakarta-sans/plus-jakarta-sans-latin-700-normal.woff2" 
  crossorigin="anonymous" />

<!-- Hero-изображение для мобиле (LCP) -->
<link rel="preload" as="image" 
  href="/images/armax_hero_9x16_right.webp" 
  media="(max-width: 767px)" />

<!-- Hero-постер для desktop/tablet (LCP) -->
<link rel="preload" as="image" 
  href="/images/ship.webp" 
  media="(min-width: 768px)" />
```

**Результат**:
- ✅ Критические шрифты загружаются раньше → меньше CLS (Layout Shift)
- ✅ Hero-изображения загружаются приоритетно → быстрее LCP
- ✅ Медиа-запросы обеспечивают загрузку только нужных ресурсов

## Ожидаемые улучшения в PageSpeed Insights

### Мобильные устройства
- 🚀 **LCP**: Улучшение на 0.5-1.5с (за счет preload изображения)
- 🚀 **Total Blocking Time**: Снижение на 200-500мс (нет загрузки видео)
- 🚀 **Transferred Size**: Уменьшение на 2-5 МБ (видео не загружается)
- 🚀 **Number of Requests**: -2 запроса (убрали webm и mp4)

### Desktop/Tablet
- 🚀 **LCP**: Улучшение на 0.3-0.8с (preload постера + metadata)
- 🚀 **Video Loading**: Только 1 формат вместо 2
- 🚀 **CLS**: Меньше сдвигов благодаря preload шрифтов

## Проверка в PageSpeed Insights

### Как проверить:
1. Перейдите на [PageSpeed Insights](https://pagespeed.web.dev/)
2. Введите URL вашего сайта
3. Запустите анализ для **Mobile** и **Desktop**

### Что проверять:

#### Mobile (приоритет!)
- **Network Panel**: Видео НЕ должно загружаться
- **LCP**: Должен быть < 2.5s (зеленая зона)
- **Total Requests**: Уменьшение на 2 запроса

#### Desktop/Tablet
- **Network Panel**: Должен загружаться **ТОЛЬКО ОДИН** формат видео (webm ИЛИ mp4)
- **LCP**: Должен быть < 2.5s

### Как проверить локально (Chrome DevTools):

1. **Mobile test**:
   ```
   - Откройте DevTools (F12)
   - Device Toolbar (Ctrl+Shift+M) → iPhone 12 Pro
   - Network tab → Очистить → Refresh
   - Результат: НЕ должно быть compress-hero-video.*
   ```

2. **Desktop test**:
   ```
   - DevTools → Network tab → Очистить
   - Desktop view → Refresh
   - Результат: Должен быть ОДИН файл (webm ИЛИ mp4)
   - В Chrome обычно загружается webm (лучшее сжатие)
   ```

## Дополнительные рекомендации

### Для дальнейшей оптимизации:
1. ⚡ Рассмотрите сжатие видео до меньшего bitrate
2. ⚡ Используйте lazy-loading для изображений ниже fold
3. ⚡ Рассмотрите использование `<link rel="preconnect">` для внешних ресурсов
4. ⚡ Проверьте сжатие Gzip/Brotli на сервере

### Мониторинг:
- Регулярно проверяйте PageSpeed Insights
- Используйте Chrome User Experience Report (CrUX) для реальных метрик
- Мониторьте Core Web Vitals в Google Search Console

---

**Дата оптимизации**: 05.02.2026
**Версия**: 1.0
