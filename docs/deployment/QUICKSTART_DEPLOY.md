# ⚡ Deploy в 5 шагов

Быстрый старт для деплоя на Apache сервер через SSH.

## 🚀 5 шагов до деплоя

### Шаг 1: Настройте SSH ключ

```bash
ssh-keygen -t rsa -b 4096 -C "your@email.com"
ssh-copy-id -p 22 user@your-server.com
ssh user@your-server.com  # Проверьте - должно работать без пароля
```

### Шаг 2: Заполните переменные

Откройте `scripts/deploy.sh` и замените:

```bash
REMOTE_USER="__CHANGE__"  →  REMOTE_USER="your_username"
REMOTE_HOST="__CHANGE__"  →  REMOTE_HOST="your-server.com"
REMOTE_PATH="__CHANGE__"  →  REMOTE_PATH="/var/www/html"
SSH_PORT="22"             →  SSH_PORT="22"  # или ваш порт
```

### Шаг 3: Проверьте тестовый прогон

```bash
npm run deploy:dry-run
```

Должны увидеть список файлов, которые будут загружены.

### Шаг 4: Деплой!

```bash
npm run deploy
```

### Шаг 5: Проверьте результат

```bash
curl -I http://your-server.com
# или откройте в браузере
```

## ❌ Ошибки? Читайте:

- **"Permission denied"** → Вернитесь к шагу 1
- **"REMOTE_USER not configured"** → Вернитесь к шагу 2
- **"rsync not found"** → Установите: `sudo apt install rsync`
- **404 на подстраницах** → Проверьте Apache: `AllowOverride All` и `a2enmod rewrite`

## 📚 Подробнее:

- [DEPLOY.md](./DEPLOY.md) - Шпаргалка
- [README.md](./README.md#-деплой) - Полная документация
- [DEPLOY_SYSTEM.md](./DEPLOY_SYSTEM.md) - Технический отчет

---

**🎯 Результат**: Автоматический деплой за 2 секунды (`npm run deploy`)
